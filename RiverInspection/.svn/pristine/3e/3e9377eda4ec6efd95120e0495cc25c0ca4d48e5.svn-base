package com.vk.mapper;

import com.vk.model.JointRiverLevelKey;
import org.springframework.stereotype.Repository;

@Repository
public interface JointRiverLevelMapper {
    int deleteByPrimaryKey(JointRiverLevelKey key);

    int insert(JointRiverLevelKey record);

    int insertSelective(JointRiverLevelKey record);

    int deleteRiverLevel(Integer riverId);
}